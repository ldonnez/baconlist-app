import styled from "@emotion/styled"
import SnackbarContent from "@material-ui/core/SnackbarContent"

export const StyledSnackbarContent = styled(SnackbarContent)(props => {
  return {
    backgroundColor: "white",
    color: "black"
  }
})
