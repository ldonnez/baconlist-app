import styled from "@emotion/styled"
import Chip from "@material-ui/core/Chip"

export const StyledChip = styled(Chip)`
  width: 100%;
  margin: 4px 0 4px 0;
  border-radius: 4px;

  svg:hover {
    color: rgba(85, 243, 199, 0.64)
  }
`
